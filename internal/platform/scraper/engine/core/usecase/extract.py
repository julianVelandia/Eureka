import internal.platform.scraper.engine.core.entity.path as p


class UseCaseExtract:
    def __init__(self, path):
        self.path = path

    def execute(self, path):
        if !vu.validate(path):
            pass

